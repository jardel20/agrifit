% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ajustar_cubico.R
\name{ajustar_cubico}
\alias{ajustar_cubico}
\title{Ajusta o Modelo Cúbico}
\usage{
ajustar_cubico(dose, ..., verbose = TRUE)
}
\arguments{
\item{dose}{Vetor numérico com níveis do fator (X).}

\item{...}{Uma ou mais respostas (vetores Y nomeados). Exemplo: \code{MS = materia_seca}.}

\item{verbose}{Lógico. Gera prints detalhados (padrão: TRUE).}
}
\value{
Lista com:
\describe{
\item{resultados}{Data frame: b0, b1, b2, b3, R², AIC, BIC, RMSE, p-valores, IC, etc. para cada resposta.}
\item{modelos}{Lista dos objetos \code{lm} para cada resposta.}
\item{equacoes}{Strings formatadas das equações.}
\item{graficos}{Lista de gráficos ggplot2 com observações e curva ajustada para cada resposta.}
}
}
\description{
Implementa o ajuste do modelo de regressão cúbica: Y = b0 + b1 * X + b2 * X^2 + b3 * X^3.
}
\examples{
\dontrun{
dose <- c(0, 50, 100, 150, 200, 250)
resposta <- c(10, 18, 26, 29, 31, 32)

resultado <- ajustar_cubico(
  dose = dose,
  Y = resposta,
  verbose = TRUE
)

print(resultado$resultados)
print(resultado$equacoes)
}

}
