% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ajustar_raiz_quadratica.R
\name{ajustar_raiz_quadratica}
\alias{ajustar_raiz_quadratica}
\title{Ajusta o Modelo Raiz Quadrático}
\usage{
ajustar_raiz_quadratica(dose, ..., verbose = TRUE)
}
\arguments{
\item{dose}{Vetor numérico com níveis do fator (X).}

\item{...}{Uma ou mais respostas (vetores Y nomeados). Exemplo: \code{MS = materia_seca}.}

\item{verbose}{Lógico. Gera prints detalhados (padrão: TRUE).}
}
\value{
Lista com:
\describe{
\item{resultados}{Data frame: b0, b1, b2, R², AIC, BIC, RMSE, p-valores, IC, etc. para cada resposta.}
\item{modelos}{Lista dos objetos \code{lm} para cada resposta.}
\item{equacoes}{Strings formatadas das equações.}
\item{graficos}{Lista de gráficos ggplot2 com observações e curva ajustada para cada resposta.}
}
}
\description{
Implementa o ajuste do modelo de regressão raiz quadrática: Y = b0 + b1 * sqrt(X) + b2 * X.
}
\examples{
\dontrun{
dose <- c(0, 25, 50, 100, 150, 200)
resposta <- c(10, 16, 20, 26, 30, 33)

resultado <- ajustar_raiz_quadratica(
  dose = dose,
  Y = resposta,
  verbose = TRUE
)

print(resultado$resultados)
print(resultado$equacoes)
}

}
